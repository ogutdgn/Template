import React from 'react';
import Layout from '../components/Layout/Layout';

const FileManager = () => {
  return (
    <Layout>
      <h2>FileManager</h2>
    </Layout>
  )
}

export default FileManager